[build]
  base    = "."
  command = "pnpm install --frozen-lockfile && pnpm --filter @workspace/campaign run build"
  publish = "artifacts/campaign/dist/public"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
